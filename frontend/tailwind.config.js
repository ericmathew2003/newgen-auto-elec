/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}" // only scan React src folder
  ],
  darkMode: false, // Disable dark mode completely
  theme: {
    extend: {},
  },
  plugins: [],
}
